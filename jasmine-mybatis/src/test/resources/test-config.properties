# JDBC驱动
database.driver=${db.driver:org.h2.Driver}

# 连接URL
database.url=${db.url:jdbc:h2:mem:test_jasmine;DB_CLOSE_DELAY=-1;DATABASE_TO_UPPER=false}

# 用户名
database.username=${db.username:}

# 密码
database.password=${db.password:}
